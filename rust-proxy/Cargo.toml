[package]
name = "rotate-proxy"
version = "1.0.0"
edition = "2021"
description = "Transparent TCP forwarding proxy with residential IP rotation"

[dependencies]
tokio = { version = "1", features = ["full"] }
